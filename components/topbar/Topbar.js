import Navbar from '@/components/topbar/Navbar';

export default function Topbar() {
  return (
    <header className="flex items-center p-5 bg-gray-900 transition-all duration-200">
      <Navbar />
    </header>
  );
}
